variant_name: topogate_v12_nomix
mask_loss_weight: 0.1
mask_loss_mode: additive
decoder_mode: legacy_mask_conditioned
topology_enabled: false
topology_mode: self_null
lambda_topology: 0.0
rank_loss_weight: 0.0
rank_margin: 0.1
topology_warmup_epochs: 20
topology_ramp_epochs: 10
self_init_weight: 0.8
neighbor_k: 5
gate_temperature: 1.0
description: >-
  Strict V12 NoMix control. No graph is constructed and training uses only
  clean masked reconstruction. Rank loss is explicitly disabled.
