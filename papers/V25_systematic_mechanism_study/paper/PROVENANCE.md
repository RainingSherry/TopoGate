# V25 LaTeX Asset Provenance

These tables, the figure manifest copy, and the bibliography copy are generated from the frozen PaperEvidence bundle. No model, label, checkpoint, or training log is read by the generator. See `tables/latex_assets_manifest.json` and `figures/figure_manifest.json` for source SHA256 values.
