# learned-relation-rule probe — publication manifest

Included: the independent protocol, pre-registration, S0 freeze decision,
contract scripts/tests, and the compact `S0_freeze` JSON audit bundle.

Excluded: raw inputs, labels, candidate graphs, feature tables, OOF scores,
embeddings, predictions, checkpoints, weights, logs, caches and bytecode.
No A1 performance result is published because A1 has not run.
