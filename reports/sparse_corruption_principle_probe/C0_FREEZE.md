# C0 Freeze — sparse_corruption_principle_probe

This is a protocol and apparatus freeze. It authorizes no new model training and produces no
performance result.

## Frozen contract

```yaml
project_id: sparse_corruption_principle_probe
protocol_id: sparse_corruption_principle_probe_c0_v1
old_projects_read_only:
  - adaptive_corruption_probe
  - learned_relation_rule_probe
  - representation_consumer_probe
  - relation_selection_probe
development_panel: [Mouse_retina, Baron Human, Campbell]
principles: [P0_Random, P1_SupportPreserve, P2_SupportTarget, P3_FrequencyAware, P4_ResidualHard, P5_GeometryHard]
geometry_safe_status: paired_negative_control_fixture_only
primary_seeds: [42, 123, 7]
material_delta_ari: 0.03
legal_gpu_pool: [1, 2, 3, 4, 5, 6]
forbidden_gpu_ids: [0, 7]
labels_used_during_fit: false
authorized_stages: [C0_freeze, C1_structural_audit, C2_static_library_implementation]
locked_stages: [C2_54_run_matrix, C3_holdout_runs, adaptive_policy, GAN, learned_generator]
holdout_status: membership_frozen_before_C2_results_required
```

## Admission checklist

- [x] old Relation and B1 projects are read-only;
- [x] three development roles are inherited and not outcome-selected;
- [x] H0 support proxy and exact per-row budget are explicit;
- [x] six primary principles and one fixture-only negative control are explicit;
- [x] support/value/geometry diagnostics are label-free;
- [x] toy S/V/M fixture separates apparatus validation from real-data evidence;
- [x] GPU allow/deny list is explicit;
- [x] publication exclusions are explicit;
- [x] toy audit, exact-budget tests and holdout manifest audit passed; the 54-run matrix remains locked pending explicit stage admission.

The machine-readable authority is `scripts/sparse_corruption_principle_probe/protocol.py`.
