# corruption_objective_compatibility_probe — final snapshot

`FINAL/` contains the compact, auditable publication snapshot for the frozen
`corruption_objective_compatibility_probe_e0_e4_v1` protocol.

Status: `completed_valid`  
Terminal decision: `STOP_GENERAL_CORRUPTION`

The snapshot retains only the terminal decision, aggregate dataset rows,
gate/audit records, and their manifest. It intentionally excludes raw data,
labels, arrays, embeddings, predictions, weights, checkpoints, logs, and
per-run intermediate directories. See the paired
[report](../../reports/corruption_objective_compatibility_probe/README.md)
for interpretation and evidence boundaries.
