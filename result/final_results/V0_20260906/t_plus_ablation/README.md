# V0-T paired mechanism ablations

The core panel is pre-registered as 8 datasets x 15 variants x 5 seeds. All fits use X only and external KMeans reads labels after embedding export. `reuters` is retained as a boundary condition. Non-completed cells stay in the audit CSV.
