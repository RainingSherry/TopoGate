# Representation-Consumer Probe result root

This directory contains the weight-free metadata for the completed formal S0
contract replay, S1-v2 opportunity-only Spectral matrix, and conditional S2
SimpleCut confirmation. Raw arrays, sparse graphs, checkpoints, histories,
and launch logs remain on the local result disk and are not published. Stage
directories and artifacts follow `reports/representation_consumer_probe/PROTOCOL.md`;
smoke outputs are not part of the result fact table.

The `artifact_hashes.json` files retain hashes and relative member names for
the audited local result trees. Published path fields use local-root
placeholders rather than workstation paths.
