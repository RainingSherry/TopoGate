"""Independent validation of the frozen C2 SupportTarget observation.

This project is deliberately separate from ``sparse_corruption_principle_probe``.
The latter is read-only evidence; this package adds only the pre-registered
magnitude-matched control and its audit.
"""

